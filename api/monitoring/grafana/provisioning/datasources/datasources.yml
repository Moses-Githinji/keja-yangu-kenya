apiVersion: 1

# List of datasources to insert/update
# This file is managed by Grafana's provisioning system

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
    version: 1
